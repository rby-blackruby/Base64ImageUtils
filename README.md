# Base64ImageUtils

A small java utility class to help process images stored in base64 encoded string format.

Currently supports:
- Converting base64 encoded strings to byte[] and BufferedImage.
- Merging images vertically or horizontally.

